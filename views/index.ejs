<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>توثيق ديسكورد</title>
  <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="https://logodownload.org/wp-content/uploads/2017/11/discord-logo-4-1.png">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Amiri', serif;
      height: 100vh;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #000;
      position: relative;
      color: #ffffff;
    }

    body::before {
      content: "";
      position: absolute;
      width: 200%;
      height: 200%;
      background: radial-gradient(#fff 1px, transparent 1px);
      background-size: 20px 20px;
      animation: stars 100s linear infinite;
      z-index: 0;
      opacity: 0.2;
    }

    @keyframes stars {
      from { transform: translate(0, 0); }
      to { transform: translate(-500px, 500px); }
    }

    .box {
      position: relative;
      z-index: 1;
      background: rgba(0, 0, 0, 0.7);
      border: 2px solid #00ffff;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 0 30px rgba(0, 255, 255, 0.5);
      text-align: center;
      max-width: 400px;
      animation: fadeIn 2s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    img {
      width: 180px;
      border-radius: 10px;
      margin-bottom: 20px;
      box-shadow: 0 0 15px #00ffff;
    }

    .btn {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 25px;
      font-size: 18px;
      font-weight: bold;
      border: none;
      border-radius: 8px;
      background-color: #00ffff;
      color: #000;
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff inset;
      transition: all 0.3s ease-in-out;
    }

    .btn:hover {
      background-color: #00cccc;
      color: #000;
      box-shadow: 0 0 15px #00ffff, 0 0 30px #00ffff inset;
    }

    h2 {
      margin-top: 25px;
      font-size: 22px;
      color: #ff4b4b;
      text-shadow: 0 0 10px #ff4b4b;
    }

    p {
      font-size: 16px;
      color: #d0ffff;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="box">
    <!-- صورة التحقق -->
    <img src="https://media1.giphy.com/media/v1.Y2lkPTc5MGI3NjExN2Q4bXZpbXhpZTd0dzBuZW5xZXQ5cGg1MDhmbWhwbXg4YW9lNnlneiZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9cw/DXisc911zf0sGw76Sz/giphy.gif" alt="Verification">

    <!-- زر التوثيق -->
    <a href="/login" class="btn">توثيق</a>

    <!-- رسالة للمستخدم غير الموثق -->
    <h2>غير موثوق</h2>
    <p>أنت غير موثوق حالياً، اضغط على زر "توثيق" للمتابعة.</p>
  </div>

  <script>
    window.onload = () => {
      const urlParams = new URLSearchParams(window.location.search);
      const code = urlParams.get('code');

      if (code) {
        const xhr = new XMLHttpRequest();
        xhr.open("POST", "/auth/discord/callback");
        xhr.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
        xhr.onload = () => {
          if (xhr.status === 200) {
            console.log("تم التوثيق بنجاح");
          } else {
            console.log("فشل التوثيق");
          }
        };
        xhr.send("code=" + code);
      }
    };
  </script>
</body>
</html>
